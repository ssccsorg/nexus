name: Deploy Gateway

on:
  push:
    branches: ["main"]
    paths:
      - "gateway/**"
      - ".github/workflows/deploy.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "gateway/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:
  # Placeholder: ssccs core repo → R2 업로드 후 트리거 (VPS 준비되면 활성화)
  # repository_dispatch:
  #   types:
  #     - ssccs-poc-agent-af
  #     - ssccs-docs-agent-af

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # PR check: verify npm install works (no deploy on PR)
  check-module-hub:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gateway/module-hub
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: gateway/module-hub/package-lock.json
      - run: npm ci

  check-af-sync:
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gateway/af-sync
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: gateway/af-sync/package-lock.json
      - run: npm ci

  # Push deploy: only on main merge
  deploy-module-hub:
    if: ${{ github.event_name == 'push' && (contains(github.event.head_commit.modified, 'gateway/module-hub/') || contains(github.event.head_commit.added, 'gateway/module-hub/')) }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gateway/module-hub
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: gateway/module-hub/package-lock.json
      - run: npm ci
      - name: Deploy module-hub
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          workingDirectory: gateway/module-hub
          command: deploy

  deploy-af-sync:
    if: ${{ github.event_name == 'push' && (contains(github.event.head_commit.modified, 'gateway/af-sync/') || contains(github.event.head_commit.added, 'gateway/af-sync/')) }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: gateway/af-sync
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
          cache-dependency-path: gateway/af-sync/package-lock.json
      - run: npm ci
      - name: Deploy af-sync
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          workingDirectory: gateway/af-sync
          command: deploy
