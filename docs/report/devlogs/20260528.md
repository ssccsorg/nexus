# 2026-05-28 — CompositeColdStorage: 3-tier orchestrated cold storage (Phase 1 final)

CompositeColdStorage is a platform-independent cold storage backend that implements the full `ColdStorage` trait by orchestrating three independent storage tiers:

- **Tier 1 (KV) — `KeyValueStore`**: fast single-key read/write, recent buffer, cursor persistence
- **Tier 2 (Blob) — `BlobStore`**: JSON-lines archive, bulk scan, flush target
- **Tier 3 (Object) — `ObjectStore`**: CAS-based coordination via `get_state`/`put_state`

## Architecture

```
                 ColdStorage trait
                        |
              CompositeColdStorage<K, B, O, C>
                    /       |        \
                KV        Blob      Object
              (recent)   (archive)   (CAS coord.)
                    \       |        /
              Workers KV   R2    Durable Object
              Sled       FS       Redis lock
              MockKv    MockBlob   MockObject
```

Each tier is a trait that external WASM bindings implement. `CompositeColdStorage` itself contains zero platform-specific code.

## Data flow (final)

1. **Ingest**: `submit_fact` writes to KV only (Tier 1).
2. **Claim**: `claim_intent` atomically gates on `object.put_state(key, "", agent)`. Only one worker passes the CAS. Then updates KV for consistency. Failed CAS returns Conflict.
3. **Flush**: `flush_since` iterates KV keys one by one, filters by submission timestamp (Stamped submitted_at), serializes matching entries to JSON-lines inline, writes to Blob (Tier 2). No intermediate Vec<Stamped> allocation — streaming by design.
4. **Read**: `read_state` reads KV only (recent). `scan_partition` merges KV (recent) + Blob (flushed) with dedup by entity ID. Partial/corrupt blobs trigger `log::warn!` with blob key and error detail.
5. **Evict**: `evict_before` on DualStorage delegates to both hot (petgraph) and cold (composite). Composite evicts Blob entries by timestamp. Hot evicts stale intents. Both results summed.
6. **Restart**: `create_blackboard_from_snapshot_with_cold(snapshot, cold)` restores hot petgraph, flush cursor, and claims while accepting an external cold backend (fresh Composite after WASM restart).

## Milestone: Phase 1 final

| What | Count | Details |
|------|-------|---------|
| **Composite unit tests** | 30 | CRUD, lifecycle, flush, evict, filter, scan, CAS get_state/put_state, concurrent claim exactly 1 success |
| **Petgraph + Composite integration** | 9 | DualStorage wiring, Cypher routing, incremental flush, snapshot roundtrip, multi-entity lifecycle, eviction (hot+cold), multi-lifetime data preservation, re-flush after evict |
| **DuckDB (ext)** | 118 | Unchanged, server-only C-bindings |
| **SQLite** | 31+25 | EvictCapable added to both normalized + legacy storage |
| **Full run.sh** | core + ext + gateway + playbooks | All pass |

## Completed actions (P1 + P2 + P3)

| Priority | Action | Detail |
|----------|--------|--------|
| P1 | ObjectStore CAS integration | `set_state` + `compare_and_swap` unified into `put_state(key, expected, new)`. All writes are CAS. No unconditional write path. |
| P1 | `claim_intent` atomic gate | `object.put_state(key, "", agent)` before KV update. CAS fail → Conflict, KV missing after CAS win → rollback + NotFound. |
| P1 | `flush_since` streaming | Iterates KV keys one-by-one — no intermediate `Vec<Stamped>` allocation. Critical for WASM 128MB heap. |
| P2 | Partial blob resilience | `read_flushed_*` uses `match Err(e) => log::warn!(blob_key, e)`. Previously silent skip. |
| P2 | DualStorage evict hot+cold | `EvictCapable`: approximate_size and evict_before now delegate to both hot AND cold. |
| P2 | `ColdStorage` gains `EvictCapable` | Trait bound added. All 5 impls updated: Composite, NullStorage, DuckDB, SqlNormalized, SqliteLegacy. |
| P3 | `from_snapshot_with_cold` | New factory: `DefaultBlackboard::from_snapshot_with_cold(snap, cold)` and `create_blackboard_from_snapshot_with_cold()`. Canonical worker restart pattern. |

## Key interface decisions

`ObjectStore::put_state(key, expected, new)` — a single CAS method covering all coordination patterns:

| Call | Meaning |
|------|---------|
| `put_state(k, "", agent)` | First claim (expected is empty → key does not exist) |
| `put_state(k, old, new)` | Ownership transfer |
| `put_state(k, val, "")` | Release (empty new → delete key) |

`get_state(key)` is the only read path. No `set_state` — no unconditional write, all writes are CAS.

## WASM Worker restart canonical pattern

```rust
// 1. Load snapshot from R2
let snapshot: StorageSnapshot = r2.get("snapshot:project").await?;

// 2. Fresh WASM bindings
let kv: KeyValueStore = Namespace::new(env, "KV")?;
let blob: BlobStore = R2Bucket::new(env, "R2")?;
let object: ObjectStore = DurableObject::new(env, "OBJECT")?;

// 3. Composite Cold + Petgraph Hot from snapshot
let cold = CompositeColdStorage::new(kv, blob, object, JsDateClock, "project");
let bb = create_blackboard_from_snapshot_with_cold(snapshot, Box::new(cold));

// 4. Resume incremental flush
bb.flush_since(&bb.to_snapshot().flush_cursor)?;
```

## Changed files (net +383 lines across 13 files)

| Area | Files | Net delta |
|------|-------|-----------|
| `core/storage/composite/` | 4 | +203 / -187 |
| `core/graph/` (factory + from_snapshot_with_cold) | 2 | +32 / -0 |
| `core/model/` (ColdStorage + EvictCapable bound) | 2 | +11 / -2 |
| `core/storage/sqlite/` (EvictCapable for both storages) | 2 | +28 / -2 |
| `ext/storage-duckdb/` (EvictCapable impl) | 1 | +28 / -0 |
| `core/graph/tests/composite_integration.rs` | 1 | +264 / -0 |
| `core/composite/tests/composite_tests.rs` | 1 | +16 / -8 |

## File structure (final)

```
core/storage/composite/
  ├── Cargo.toml                        (package: nexus-storage-kv-cold, dir: composite/)
  ├── src/
  │   ├── lib.rs                        (KeyValueStore, BlobStore, ObjectStore, Now traits)
  │   ├── composite.rs                  (CompositeColdStorage<K, B, O, C> — full ColdStorage impl)
  │   └── mock.rs                       (MockKv, MockBlob, MockObject with keyed CAS)
  └── tests/
       └── composite_tests.rs           (30 tests — CAS, concurrent, lifecycle, flush)

core/graph/
  ├── src/lib.rs                        (4 factory functions including with_cold)
  └── tests/
       └── composite_integration.rs     (9 integration tests)

core/model/src/storage/
  ├── aggregate.rs                      (ColdStorage gains EvictCapable bound)
  ├── dual.rs                           (evict delegates to hot + cold)
  ├── null.rs                           (unchanged)
  └── evict.rs                          (unchanged)

core/storage/sqlite/src/
  ├── sql_normalized.rs                 (EvictCapable: SQL COUNT + no-op evict)
  └── sql_legacy.rs                     (EvictCapable: no-op)

ext/storage-duckdb/src/lib.rs           (EvictCapable: file-count + no-op evict)
```

## Next

- CF Workers WASM bindings: `KeyValueStore` for `worker::kv::Namespace`, `BlobStore` for R2, `ObjectStore` for Durable Object.
- `Now` impl for `js_sys::Date`.
