[package]
name = "nexus-verify-support"
version = "0.0.1"
edition.workspace = true
license.workspace = true
publish = false
description = "Shared support software for the nexus verification topics: a no_std flat key-space IO stand-in for the launcher-provided FileIo, and a minimal block_on for driving the storage core without an executor."

[dependencies]
chton = { workspace = true, default-features = false }
