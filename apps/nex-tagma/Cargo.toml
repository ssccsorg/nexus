[package]
name = "nex-tagma"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "synTagma coordinate PoC — SHA256-free identity generation using Hangul syllable space"

[dependencies]
tagma-core = { git = "https://github.com/ssccsorg/syntagma", branch = "main" }
sha2 = "0.10"
