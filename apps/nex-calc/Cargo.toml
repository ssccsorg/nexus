[package]
name = "nex-calc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "FIH-based calculator — computation as state space traversal"

[dependencies]
nexus-model = { path = "../../model" }
