//! nex-zed — neXus instance embedding ACP as one of its communication surfaces.
//!
//! Thin wrapper around `acp_bridge` crate adding neXus FIH integration.
//! The `acp_bridge` crate (at `apps/nex-zed/acp-bridge/`) provides the
//! complete ACP agent engine, LLM client, and tools — independently usable
//! without neXus.
